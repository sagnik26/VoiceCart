import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function CartScreen() {
  return (
    <ScreenPlaceholder
      title="Your cart"
      description="Line items, totals, plan impact, and Place order."
      showBack
    />
  );
}
