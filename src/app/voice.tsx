import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function VoiceScreen() {
  return (
    <ScreenPlaceholder
      title="Voice"
      description="Listening orb, transcript, and Show cart CTA will live here."
      showBack
    />
  );
}
