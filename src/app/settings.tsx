import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function SettingsScreen() {
  return (
    <ScreenPlaceholder
      title="Settings"
      description="Account, address, voice language, and preferences."
      showBack
    />
  );
}
